
import styled from 'styled-components';

const ConfectionsContainer = ({ className }) => {

    return (
        <div className={className}>
            {/* ЗДЕСЬ БУДЕТ СПИСОК ИЗДЕЛИЙ */}
        </div>
    )
};

export const Confections = styled(ConfectionsContainer)`
`;
