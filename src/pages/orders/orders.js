
import styled from 'styled-components';

const OrdersContainer = ({ className }) => {

    return (
        <div className={className}>
            {/* ЗДЕСЬ БУДЕТ СПИСОК ИЗДЕЛИЙ */}
        </div>
    )
};

export const Orders = styled(OrdersContainer)`
`;
